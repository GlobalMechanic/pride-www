{
  "name": "pride-www",
  "version": "0.0.1",
  "main": "dist",
  "scripts": {
    "update": "npx ncu -u -a",
    "lint": "eslint src --fix",
    "test": "mocha --options .mocha.opts",
    "test:dev": "npx watch 'npm run test' src",
    "babel": "rm -rf dist; mkdir dist; babel src --out-dir dist --copy-files",
    "babel:dev": "npm run babel -- --watch",
    "webpack": "NODE_ENV=production webpack",
    "webpack:dev": "NODE_ENV=development webpack-dev-server",
    "serve": "node ./dist/scripts/serve.js",
    "serve:dev": "NODE_ENV=development nodemon --watch dist --watch config ./dist/scripts/serve.js",
    "build": "npm i && npm run lint && npm run babel && npm run webpack && npm run test",
    "start": "NODE_ENV=production npm run serve"
  },
  "keywords": [],
  "author": "BenZed",
  "license": "ISC",
  "description": "generated by BenZed's create-project dev tool",
  "dependencies": {
    "@benzed/app": "^0.5.4",
    "@benzed/react": "^0.2.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-markdown": "^3.4.1",
    "react-router-dom": "^4.3.1",
    "styled-components": "^3.3.3"
  },
  "devDependencies": {
    "@benzed/dev": "^0.8.0"
  }
}
